{
  "name": "groedu-dev",
  "private": true,
  "description": "Local convenience only — gitignored. Starts all three services together.",
  "scripts": {
    "dev": "node dev.mjs",
    "stop": "node dev.mjs --stop",
    "dev:api": "pnpm --dir backend run start:dev",
    "dev:admin": "pnpm --dir frontend/admin run dev",
    "dev:learner": "pnpm --dir frontend/learner run dev",
    "install:all": "pnpm --dir backend install && pnpm --dir frontend/admin install && pnpm --dir frontend/learner install",
    "typecheck": "cd backend && npx tsc --noEmit && cd ../frontend/admin && npx tsc --noEmit && cd ../learner && npx tsc --noEmit",
    "test": "pnpm --dir backend run test",
    "db:migrate:tenancy": "pnpm --dir backend run db:migrate:tenancy",
    "db:verify:isolation": "pnpm --dir backend run db:verify:isolation"
  }
}
